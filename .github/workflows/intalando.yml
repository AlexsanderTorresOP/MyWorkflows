name: Primeiro Workflow Com Install
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  PrimeiroJob:
    runs-on: ubuntu-latest
    steps:
      - name: Testando Install
        run: |
          sudo apt install git
          git -v
